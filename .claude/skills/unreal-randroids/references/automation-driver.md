# Automation Driver

- Built-in input simulation and locator API.
- Common locators: `By::Id("...")` and `By::Path("...")`.
- Use for UI interactions like click, type, and key sequences.
- Stable automation IDs make selectors robust.
